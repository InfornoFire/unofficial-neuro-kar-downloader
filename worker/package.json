{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "biome check src --write && biome format src --write",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  }
}
