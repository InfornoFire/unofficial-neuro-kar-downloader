# ***COPY AND REMOVE .template EXTENSION***
# URL of the deployed Cloudflare Worker.
# For local dev, run `pnpm dev` in the worker/ directory (default: http://localhost:8787)
VITE_WORKER_URL=https://neuro-kar-proxy.<your-subdomain>.workers.dev
